NEXT_PUBLIC_API_URL=http://localhost:8000
# Only needed if you run the backend with RESEARCH_AGENT_TOKEN set (e.g. when
# exposing it to the LAN via research-agent-view). Leave unset for plain
# localhost-only use.
NEXT_PUBLIC_API_TOKEN=
